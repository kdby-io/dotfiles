# fzf
# [ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

alias v="nvim"
# alias ls="lsd"
alias git=hub
alias dc=docker-compose

set -x THEFUCK_OVERRIDDEN_ALIASES 'nvim,lsd,hub,docker-compose'
set -gx PROJECT_PATHS ~/Documents