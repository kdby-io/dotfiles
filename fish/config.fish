set theme_color_scheme gruvbox

alias v="nvim"
alias ls="lsd"

# fzf
# [ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

alias git=hub
alias dc=docker-compose

set -x THEFUCK_OVERRIDDEN_ALIASES 'hub,docker-compose'